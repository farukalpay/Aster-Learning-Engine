#ifndef NETIO_DETAIL_ARRAY_HPP
#define NETIO_DETAIL_ARRAY_HPP

#if defined(_MSC_VER) && (_MSC_VER >= 1200)
# pragma once
#endif // defined(_MSC_VER) && (_MSC_VER >= 1200)

#include "netio/detail/config.hpp"

#include <array>

namespace netio {
namespace detail {

using std::array;

} // namespace detail
} // namespace netio

#endif // NETIO_DETAIL_ARRAY_HPP
