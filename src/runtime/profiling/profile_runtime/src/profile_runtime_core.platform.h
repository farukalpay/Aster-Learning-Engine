#pragma once
#include "profile_runtime.config.h"

#if USE_PROFILE_RUNTIME

#include "profile_runtime_common.h"
#include "profile_runtime_memory.h"

//////////////////////////////////////////////////////////////////////////
// Platform-specific stuff
//////////////////////////////////////////////////////////////////////////
namespace ProfileRuntime
{
	struct Trace;
	struct Module;
	struct Symbol;
	struct SymbolEngine;

	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	// Platform API
	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	struct Platform
	{
		// Platform Name
		static PROFILE_RUNTIME_INLINE const char* GetName();
		// Thread ID (system thread id)
		static PROFILE_RUNTIME_INLINE ThreadID GetThreadID();
		// Process ID
		static PROFILE_RUNTIME_INLINE ProcessID GetProcessID();
		// CPU Frequency
		static PROFILE_RUNTIME_INLINE int64 GetFrequency();
		// CPU Time (Ticks)
		static PROFILE_RUNTIME_INLINE int64 GetTime();
		// System Tracer
		static PROFILE_RUNTIME_INLINE Trace* CreateTrace();
		// Symbol Resolver
		static PROFILE_RUNTIME_INLINE SymbolEngine* CreateSymbolEngine();
	};

	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	// Tracing API
	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	struct Trace
	{
		virtual void SetPassword(const char* /*pwd*/) {};
		virtual CaptureStatus::Type Start(Mode::Type mode, int frequency, const ThreadList& threads) = 0;
		virtual bool Stop() = 0;
		virtual ~Trace() {};
	};


	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	// Symbol API
	
	struct Module
	{
		string path;
		void* address;
		size_t size;
		Module(const char* p, void* a, size_t s) : path(p), address(a), size(s) {}
	};
	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	struct Symbol
	{
		uint64 address;
		uint64 offset;
		wstring file;
		wstring function;
		uint32 line;
		Symbol()
			: address(0)
			, offset(0)
			, line(0)
		{}
	};
	////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	struct SymbolEngine
	{
		// Get list of loaded modules
		virtual const vector<Module>& GetModules() = 0;

		// Get Symbol from address
		virtual const Symbol* GetSymbol(uint64 dwAddress) = 0;

		virtual ~SymbolEngine() {};
	};
}
//////////////////////////////////////////////////////////////////////////

#endif //USE_PROFILE_RUNTIME